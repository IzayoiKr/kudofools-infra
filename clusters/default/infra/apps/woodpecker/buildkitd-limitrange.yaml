apiVersion: v1
kind: LimitRange
metadata:
  name: buildkitd-limits
  namespace: buildkitd
spec:
  limits:
    - default:
        cpu: "500m"
        memory: 512Mi
      defaultRequest:
        cpu: "100m"
        memory: 64Mi
      max:
        cpu: "4"
        memory: 4Gi
      type: Container
