apiVersion: v1
kind: ConfigMap
metadata:
  name: openbao-config
  namespace: default
data:
  openbao.hcl: |
    storage "file" {
      path = "/data"
    }
    listener "tcp" {
      address     = "0.0.0.0:8200"
      tls_disable = true
    }
    api_addr = "http://openbao:8200"
